/**
 * @type {string}
 */
export const HOME = "home";

/**
 * @type {string}
 * Typedef used to throw an error if path is wrong.
 * @typedef { import('weaken.js') }
 */
export const IMPLANT_PATH_WEAKEN = "weaken.js";

/**
 * @type {string}
 * Typedef used to throw an error if path is wrong.
 * @typedef { import('grow.js') }
 */
export const IMPLANT_PATH_GROW = "grow.js";

/**
 * @type {string}
 * Typedef used to throw an error if path is wrong.
 * @typedef { import('hack.js') }
 */
export const IMPLANT_PATH_HACK = "hack.js";

/**
 * @type {string}
 */
export const NEW_SERVER_PREFIX = "server";
